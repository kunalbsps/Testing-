import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ngrx',
  appName: 'ngrx',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
